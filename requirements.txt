# Phase 1 — desktop UI and core utilities
PySide6>=6.6.0,<7
Pillow>=10.0.0

# Phase 2+ — metadata and image processing
piexif>=1.1.3
opencv-python-headless>=4.8.0,<5

# Phase 3+ — inference runtime (CPU by default)
onnxruntime>=1.16.0
onnx>=1.14.0

# Best open-source face packs for personal / non-commercial use (InsightFace).
# Pretrained InsightFace model weights are non-commercial; code is MIT.
insightface>=0.7.3

# Optional — MiVOLO v2 age backend (Settings → Age model).
# GPU (GTX 1660 Ti / CUDA 12.4 wheel):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install transformers accelerate
#   pip install "setuptools<81"
#   pip install git+https://github.com/WildChlamydia/MiVOLO.git --no-build-isolation

# Packaging (Phase 8)
pyinstaller>=6.0.0

# Development / tests
pytest>=7.4.0

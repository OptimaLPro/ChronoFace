"""
Model weights directory.

Downloaded on first analysis if missing:

- ``face_detection_yunet_2023mar.onnx`` — YuNet face detector (OpenCV Zoo)
- ``face_recognition_sface_2021dec.onnx`` — SFace face recognizer (OpenCV Zoo)
- ``age_efficientnet_b2.onnx`` — facial age estimator (FaceONNX-compatible)

ONNX weight files are gitignored. Verify licenses before commercial redistribution.
"""
